{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=development&& nodemon -r dotenv/config server.js",
    "dev": "set NODE_ENV=development&& nodemon -r dotenv/config server.js",
    "production": "set NODE_ENV=production&& nodemon -r dotenv/config server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ajv": "^8.6.3",
    "bcrypt": "^5.0.1",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.13",
    "mongoose-unique-validator": "^3.0.0",
    "multer": "^1.4.3",
    "nodemon": "^2.0.15"
  }
}
